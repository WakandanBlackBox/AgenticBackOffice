DATABASE_URL=postgresql://user:pass@localhost:5432/backoffice
ANTHROPIC_API_KEY=sk-ant-...
JWT_SECRET=generate-with-openssl-rand-hex-32
NODE_ENV=development
PORT=3000
CORS_ORIGIN=http://localhost:5173
# Optional: PEM body of the Postgres CA cert to pin against (production only).
# When unset, the system CA bundle is used. Set this if your DB provider issues
# certs from a private CA.
# PG_CA_CERT=
